apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-srv-bastion-01-sshd
  namespace: aisecops
  labels:
    app: srv-bastion-01
    service: sshd
    managed-by: aisecops-agent
data:
  sshd_config: |
    # sshd_config — srv-bastion-01
    # Ubuntu 24.04 LTS | Rôle : bastion SSH (criticité CRITIQUE)
    # CMDB : cmdb/srv-bastion-01.md
    # ATTENTION : ce serveur est le seul point d'entrée SSH vers toute l'infrastructure.

    Port 2222
    AddressFamily inet
    ListenAddress 0.0.0.0

    LoginGraceTime 30
    PermitRootLogin no
    StrictModes yes
    MaxAuthTries 2
    MaxSessions 3
    MaxStartups 3:100:5

    PubkeyAuthentication yes
    AuthorizedKeysFile .ssh/authorized_keys

    PasswordAuthentication no
    PermitEmptyPasswords no

    KbdInteractiveAuthentication no

    AllowGroups ops-team

    UsePAM yes
    AcceptEnv LANG LC_*

    X11Forwarding no
    PrintMotd no
    AllowAgentForwarding yes
    AllowTcpForwarding yes
    GatewayPorts no
    PermitTunnel no

    HostbasedAuthentication no
    IgnoreUserKnownHosts yes
    IgnoreRhosts yes

    ClientAliveInterval 300
    ClientAliveCountMax 2

    SyslogFacility AUTHPRIV
    LogLevel VERBOSE

    Subsystem sftp /usr/lib/openssh/sftp-server

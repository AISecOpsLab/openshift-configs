apiVersion: v1
kind: ConfigMap
metadata:
  name: cm-srv-dns-01-named
  namespace: aisecops
  labels:
    app: srv-dns-01
    service: bind9
    managed-by: aisecops-agent
data:
  named.conf.options: |
    // named.conf.options — srv-dns-01
    // Rôle : DNS interne (BIND9), zone aisecops.lab
    // CMDB : cmdb/srv-dns-01.md

    // Configuration cleared due to security injection alert. No options defined.
